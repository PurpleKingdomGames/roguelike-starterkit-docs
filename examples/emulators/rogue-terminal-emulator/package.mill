package build.examples.emulators.`rogue-terminal-emulator`

import indigoplugin.*
import build.RLSKModule

object `package` extends RLSKModule:
  def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
    makeIndigoOptions("Rogue Terminal Emulator Example")
