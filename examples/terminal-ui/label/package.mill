package build.examples.`terminal-ui`.label

import indigoplugin.*
import build.RLSKModule

object `package` extends RLSKModule:
  def indigoOptions(assetsDirectory: os.Path): IndigoOptions =
    makeIndigoOptions("UI Components - Label")
